// Convert Celsius-Fahrenheit
const celsius = prompt("Enter the Celsius value: ");

// Fahrenheit calculation
const fahrenheit = (celsius * 1.8) + 32

// Result log
console.log(`${celsius} degree celsius is ${fahrenheit} degree fahrenheit.`);
